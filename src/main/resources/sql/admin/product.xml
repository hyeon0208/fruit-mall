<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fruit.mall.admin.product.ProductMapper">
    <insert id="insertProduct" parameterType="com.fruit.mall.admin.product.Product" keyProperty="productId">
        INSERT INTO product (
                             CATEGORY_ID,
                             PRODUCT_NAME,
                             PRODUCT_PRICE,
                             PRODUCT_STOCK,
                             PRODUCT_DESCRIPTION,
                             PRODUCT_DISCOUNT,
                             PRODUCT_SALE_STATUS
                             )
                    VALUES (
                            #{categoryId},
                            #{productName},
                            #{productPrice},
                            #{productStock},
                            #{productDescription},
                            #{productDiscount},
                            #{productSaleStatus}
                            )
    </insert>

    <select id="selectProductAllById" parameterType="Long" resultType="com.fruit.mall.admin.product.Product">
        SELECT * FROM product WHERE PRODUCT_ID = #{productId}
    </select>

    <select id="selectAll" resultType="com.fruit.mall.admin.product.Product">
        SELECT * FROM product
    </select>

    <select id="countOnSaleProducts" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM product
        WHERE PRODUCT_SALE_STATUS = '판매중'
    </select>

    <select id="countOffSaleProducts" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM product
        WHERE PRODUCT_SALE_STATUS = '판매중지'
    </select>

    <select id="countSoldOutProducts" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM product
        WHERE PRODUCT_SALE_STATUS = '품절'
    </select>

</mapper>
