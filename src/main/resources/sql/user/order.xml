<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fruit.mall.orders.OrderMapper">
    <select id="selectOneOrderInfoByProductId" resultType="com.fruit.mall.orders.dto.OrderReqDto">
        SELECT
            p.PRODUCT_NAME,
            p.PRODUCT_PRICE,
            p.PRODUCT_DISCOUNT,
            i1.IMAGE_URL
        FROM product p
                 JOIN image i1 ON i1.PRODUCT_ID = p.PRODUCT_ID
        WHERE
            p.PRODUCT_ID = #{productId}
          AND
                i1.IMAGE_ID = (
                SELECT MIN(i2.IMAGE_ID)
                FROM image i2
                WHERE i2.PRODUCT_ID = p.PRODUCT_ID
            )
    </select>
</mapper>