<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fruit.mall.review.ReviewMapper">
    <insert id="insertReview" parameterType="com.fruit.mall.review.Review" useGeneratedKeys="true" keyProperty="reviewId">
        INSERT INTO review (PRODUCT_ID, USER_ID_NO, REVIEW_CONTENTS)
        VALUES (#{productId}, #{userIdNo}, #{reviewContents})
    </insert>

    <select id="selectReviewIdByUserIdAndProductId" resultType="Long">
        SELECT REVIEW_ID FROM review
        WHERE USER_ID_NO = #{userIdNo} AND PRODUCT_ID = #{productId}
    </select>
</mapper>